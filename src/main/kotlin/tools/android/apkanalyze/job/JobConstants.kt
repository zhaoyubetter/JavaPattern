package tools.android.apkanalyze.job

class JobConstants {
    companion object {
        val PARAM_ORDER = "--order"
        val PARAM_MIN_SIZE_IN_KB = "--min"
        val PARAM_SUFFIX = "--suffix"

        val ORDER_ASC = "asc"
        val ORDER_DESC = "desc"
    }
}